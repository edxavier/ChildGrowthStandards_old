<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/md_white_1000</item>
        <item name="android:textColorPrimary">@color/primary_text</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>

        <item name="colorControlHighlight">@color/md_grey_500_25</item>
        <item name="colorSwitchThumbNormal">@color/md_grey_300</item>

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

    </style>

    <!-- Splash Screen Theme. -->
    <style name="AppTheme.Fullscreen">
        <item name="windowNoTitle">true</item>
        <item name="android:textColorPrimary">@color/primary_dark</item>
        <item name="android:textColorSecondary">@color/secondary_text</item>v
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowBackground">@drawable/splash_drawable</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="Hint" parent="TextAppearance.Design.Hint">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="login_input_error_appearance" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/md_orange_500</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="EditText" parent="Widget.AppCompat.EditText">
        <item name="android:textSize">18sp</item>
    </style>

    <style name="MaterialButton" parent="AppTheme">
        <item name="android:textColor">@color/icons</item>
        <item name="android:padding">4dp</item>
        <item name="colorControlHighlight">@color/md_pink_200</item>
        <item name="colorButtonNormal">@color/accent</item>
    </style>

    <style name="FlatButtonDarkBackgroud" parent="Theme.AppCompat.Light">
        <item name="colorControlHighlight">@color/md_pink_300</item>
        <item name="android:textColor">@color/colorAccent</item>
    </style>

    <style name="AppTheme.Traslucent" parent="AppTheme">
        <item name="android:windowBackground">@drawable/splash_drawable</item>
    </style>

</resources>
